*** Begin Patch
*** Update File: assets/js/chatbot.js
@@
-        // Normalize ALL assistant messages: one bar each, positioned at the end
+        // Normalize ALL assistant messages: exactly one bar each, positioned at the end
         bots.forEach(msg => {
           if (msg.classList && msg.classList.contains('is-typing')) return; // skip typing ones
-          const bars = msg.querySelectorAll('.dual-msg-actions');
-          if (bars.length === 0) {
-            buildMsgActions(msg, 'bot', null, (msg.querySelector('.dual-chatbot-message-content')?.innerText || msg.innerText || contentText));
-          } else {
-            // keep the bottom-most bar and remove the rest
-            const keep = bars[bars.length - 1];
-            for (let i = 0; i < bars.length - 1; i++) bars[i].remove();
-            if (keep !== msg.lastElementChild) msg.appendChild(keep);
-          }
+          // Remove all existing bars within this assistant message and rebuild a single one
+          msg.querySelectorAll('.dual-msg-actions').forEach(b => b.remove());
+          buildMsgActions(msg, 'bot', null, (msg.querySelector('.dual-chatbot-message-content')?.innerText || msg.innerText || contentText));
+          const bar = msg.querySelector('.dual-msg-actions');
+          if (bar && bar !== msg.lastElementChild) msg.appendChild(bar);
         });
*** End Patch
